//strlen(): Returns the length of a string.

#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Ansariyaz";
    int length = strlen(str);
    printf("Length of the string: %d\n", length);
    return 0;
}