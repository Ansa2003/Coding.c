#include<stdio.h>

#include <math.h>

void main()

{

int integertype;

char chartype;

float floattype;

double doubletype;

sizeof(integertype);
sizeof(chartype); 
sizeof(floattype);
sizeof(doubletype);

printf("size of int is:%l d \n", sizeof(integertype));
printf("size of char is:%l d \n", sizeof(chartype));
printf("the size of float is: %l d \n", sizeof(floattype));
printf("the size of double is: %l d \n",sizeof(doubletype));
 }